## Building
```
make
```
## How to use
Syntax for moves is as follows:
`[fromRow][fromCol][toRow][toCol](optional: promotion choice)`

`Row`s: a-h  
`Col`s: 1-8  
`promotion choice`: Q=queen, k=knight, r=rook, b=bishop

**Syntax** ***is*** **case sensitive**
